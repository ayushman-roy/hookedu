import React from "react";

export default function User_Profile(props) {
  const { name, age, gender, batch, school, type, image_url, bio } =
    props.final_user;
  return <></>;
}

// TODO: user_profile skeleton & integrate + test
